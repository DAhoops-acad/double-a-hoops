import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: "--font-inter"
});

export const metadata: Metadata = {
  title: 'Double A Hoops Academy | Youth Basketball Training',
  description: 'Elite youth basketball training led by coaches Aarush and Aneesh. Build skills, confidence, and elevate your game. 50% of profits donated to Vibha Charity.',
  keywords: ['basketball training', 'youth basketball', 'basketball academy', 'skills training'],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background scroll-smooth">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
